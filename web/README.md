# web

Frontend goes here (React + TypeScript).

Suggested first task for Claude Code:
- Table view: list all family members, sortable columns, basic filter by
  name.
- Tree view: render the family based on `ParentChild` data. Evaluate a
  genealogy-focused layout library before hand-rolling the tree layout —
  a plain recursive-tree component will struggle once someone has two
  parents.
